extends layout

mixin message(opp, name)
    if(opp)
        h1.greeting Logged in as 
            span=name 
    


append links 
    li(style="float: right;")
        a(href="/logout") Loggout 

block content
    .dashboard 
        .user 
            +message(opp, name)